---
interface Props {
  id?: string;
  message?: string;
}

const { id = "copy-toast", message = "Промпт скопирован" } = Astro.props;
---

<div id={id} class="toast" aria-live="polite" aria-atomic="true">
  {message}
</div>
